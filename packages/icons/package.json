{
  "name": "icons",
  "version": "0.0.0",
  "description": "Transform Icons into Optimized React Component",
  "license": "ISC",
  "main": "./dist/index.ts",
  "types": "./dist/index.d.ts",
  "files": [
    "dist/*"
  ],
  "scripts": {
    "dev": "rm -rf dist && svgr -d dist -- src && tsc"
  },
  "devDependencies": {
    "@svgr/cli": "6.5.1",
    "@types/react": "18.0.27",
    "react": "18.2.0",
    "tsconfig": "workspace:*"
  },
  "pnpm": {
    "overrides": {
      "svgo": "3.0.2"
    }
  }
}