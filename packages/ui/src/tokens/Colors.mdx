import { Meta, ColorPalette, ColorItem } from '@storybook/blocks';
import tw from '../../tailwind.config';

<Meta title="tokens/Colors" />

# Colors

---

&nbsp;

<ColorPalette>
  {Object.entries(tw.theme.colors).map(([k, v]) => {
    const _colors = typeof v === 'string' ? { [k]: v } : v;
    return <ColorItem key={k} title={k} colors={_colors} />;
  })}
</ColorPalette>
